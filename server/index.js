//   Y50Cbt992dbme8bh        mongodb lms
//  mongodb+srv://studyprash:Y50Cbt992dbme8bh@cluster0.gpngcki.mongodb.net/

// install express mongoose cors dotenv bcryptjs jsonwebtoken nodemona
import express from "express";

const app = express();

const PORT = 8080;

app.listen(PORT, () => {
    console.log(`Server listen at port ${PORT}`);
})
